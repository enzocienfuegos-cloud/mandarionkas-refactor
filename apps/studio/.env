# Creative Studio — dev environment
# Vite proxies /v1 → http://localhost:4000 (see vite.config.ts)
# Using a relative base means the proxy intercepts all API calls.
VITE_API_BASE_URL=/v1
VITE_ASSETS_BASE_URL=
VITE_APP_ENV=development
