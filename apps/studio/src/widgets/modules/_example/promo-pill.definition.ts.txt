import { createModuleDefinition } from '../module-definition-factory';
import { renderPromoPillExample } from './promo-pill.renderer.example';
import { promoPillExportRendererExample } from './promo-pill.export.example';

export const PromoPillDefinition = createModuleDefinition({
  type: 'x-promo-pill',
  label: 'Promo Pill',
  category: 'content',
  description: 'Small badge-style module for offer callouts.',
  frame: { x: 40, y: 40, width: 220, height: 72, rotation: 0 },
  props: {
    title: 'Weekend drop',
    caption: 'Limited release',
  },
  style: {
    backgroundColor: '#102030',
    color: '#ffffff',
    borderRadius: 18,
    fontSize: 22,
    fontWeight: 700,
  },
  renderStage: renderPromoPillExample,
  renderExport: promoPillExportRendererExample.render,
  inspectorFields: [
    { key: 'title', label: 'Title', type: 'text' },
    { key: 'caption', label: 'Caption', type: 'text' },
  ],
  capabilities: {
    hasAccentColor: true,
    exposesActions: true,
  },
});
