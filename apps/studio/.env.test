# Studio — test environment
# API base must be empty so repository tests that assert
# "throws when no API base is configured" work correctly.
# Vitest picks this up automatically when running `vitest run`.
VITE_API_BASE_URL=
VITE_ASSETS_BASE_URL=
VITE_APP_ENV=test
