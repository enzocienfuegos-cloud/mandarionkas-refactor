import type { WidgetNode } from '../../domain/document/types';
import type { RenderContext } from '../../canvas/stage/render-context';
import { getAccent, moduleBody, moduleHeader, moduleShell, renderCollapsedIfNeeded } from './shared-styles';
function WeatherModuleRenderer({ node, ctx }: { node: WidgetNode; ctx: RenderContext }): JSX.Element { const condition = String(node.props.condition ?? 'Sunny'); const temperature = String(node.props.temperature ?? '28'); const theme = String(node.props.theme ?? 'auto'); const accent = getAccent(node); const icon = condition.toLowerCase().includes('storm') ? '⛈️' : condition.toLowerCase().includes('rain') ? '🌧️' : condition.toLowerCase().includes('cloud') ? '☁️' : '☀️'; return <div style={moduleShell(node, ctx)}><div style={moduleHeader(node)}>{String(node.props.title ?? node.name)}</div><div style={moduleBody}><div style={{display:'flex',alignItems:'center',justifyContent:'space-between'}}><div><div style={{fontSize:28,fontWeight:900}}>{temperature}°</div><div style={{fontSize:12,opacity:.78}}>{String(node.props.location ?? 'Location')}</div></div><div style={{fontSize:34}}>{icon}</div></div><div style={{padding:'8px 10px',borderRadius:10,background:`${accent}22`,fontSize:12}}>{condition} · {theme}</div></div></div>; }
export function renderWeatherConditionsStage(node: WidgetNode, ctx: RenderContext): JSX.Element { const collapsed=renderCollapsedIfNeeded(node,ctx); if(collapsed) return collapsed; return <WeatherModuleRenderer node={node} ctx={ctx}/>; }
