# pnpm configuration for SMX Studio monorepo

# Hoist all packages to avoid deep nesting issues
shamefully-hoist=true

# Approve build scripts for packages that require native compilation
# (esbuild bundles its own binary; bcryptjs is pure JS and needs no approval)
approve-builds[]=esbuild

# Silence the "ignored build scripts" warning
ignore-scripts=false
