SMX Studio release smoke report

Artifact date: 2026-04-12
Scope: Sprint 3 entity-sidecar persistence hardening

Checks verified in this workspace:
- server/store.mjs syntax check via `node --check` ✅
- server/r2.mjs syntax check via `node --check` ✅
- release package structure reviewed ✅
- release package excludes nested legacy archive and common junk artifacts ✅
- repo policy prerequisite files now present (`.github/workflows/ci.yml`, `docs/release/README.md`) ✅

Checks not re-executed in this workspace:
- npm run typecheck
- npm run build
- npm run release:check

Reason:
- local `node_modules/` is not installed in this workspace, so TypeScript/Vite checks cannot be rerun here without first installing dependencies

Release-specific observations:
- previous Sprint 3 zip incorrectly included `Archive3_api_r2_only_ftp_ready_v2`
- clean replacement zip excludes that nested archive
- backend persistence now writes entity sidecars for clients, projects, project states, asset folders, and assets

Recommended deployment verification on Cloudways:
- `npm ci`
- `npm run repo:policy`
- `npm run release:check`
- restart `platform:api`
