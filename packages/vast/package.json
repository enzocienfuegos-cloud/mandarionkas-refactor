{
  "name": "@smx/vast",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "VAST 4 IAB engine — parser, resolver, tracker. DOM-free, framework-agnostic.",
  "main": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@xmldom/xmldom": "^0.9.8",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
