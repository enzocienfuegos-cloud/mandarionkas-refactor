import type { WidgetNode } from '../../domain/document/types';
import type { RenderContext } from '../../canvas/stage/render-context';
import { getAccent, moduleBody, moduleHeader, moduleShell, renderCollapsedIfNeeded } from './shared-styles';
function TravelDealModuleRenderer({ node, ctx }: { node: WidgetNode; ctx: RenderContext }): JSX.Element { const accent = getAccent(node); return <div style={moduleShell(node, ctx)}><div style={moduleHeader(node)}>{String(node.props.title ?? node.name)}</div><div style={moduleBody}><div style={{ borderRadius: 12, padding: 12, background: 'linear-gradient(135deg,#0f172a,#1d4ed8)', minHeight: 72, display: 'flex', flexDirection: 'column', justifyContent: 'space-between' }}><strong style={{ fontSize: 18 }}>{String(node.props.destination ?? 'Madrid')}</strong><span style={{ fontSize: 12, opacity: .82 }}>{String(node.props.airline ?? 'Iberia')} · {String(node.props.nights ?? '4 nights')}</span></div><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'end' }}><div><div style={{ fontSize: 12, opacity: .72 }}>From</div><div style={{ fontWeight: 900, fontSize: 24 }}>{String(node.props.price ?? '$499')}</div></div><button type="button" onClick={(event) => { event.stopPropagation(); ctx.triggerWidgetAction('click'); }} style={{ borderRadius: 12, border: 'none', background: accent, color: '#111827', fontWeight: 800, padding: '10px 12px', cursor: 'pointer' }}>{String(node.props.ctaLabel ?? 'Book now')}</button></div></div></div>; }
export function renderTravelDealStage(node: WidgetNode, ctx: RenderContext): JSX.Element { const collapsed=renderCollapsedIfNeeded(node,ctx); if(collapsed) return collapsed; return <TravelDealModuleRenderer node={node} ctx={ctx}/>; }
